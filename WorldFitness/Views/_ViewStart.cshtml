@{
    string currentUser=ViewContext.RouteData.Values["controller"].ToString();
    Console.WriteLine("here: ", currentUser);
    @if(currentUser == "Home")
    {
       Layout = "_Layout";
    }
    else if(currentUser == "Admin")
    {
        Layout = "_AdminLayout";
    }
    else
    {
        Layout = "_CustomerLayout";
    }
}
